# Duty Data

此資料夾包含所有副本提示資料，每個副本一個 `.json` 檔。

- `_schema.json`：JSON Schema 規範
- 其他 `.json` 檔：副本內容

## 新增副本

1. 複製一個現有的 JSON 檔案
2. 修改 `id`（請確保獨一無二、用小寫英數底線）
3. 編輯內容
4. 送 PR ✨

詳細格式請看 [`docs/duty-format.md`](../../docs/duty-format.md)。
